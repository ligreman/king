import { ValidatorNumberDirective } from './custom-validators';

describe('ValidatorNumberDirective', () => {
    it('should create an instance', () => {
        const directive = new ValidatorNumberDirective();
        expect(directive).toBeTruthy();
    });
});
